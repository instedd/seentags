.btn-group.pull-right
  - if !@account.nil? && !@account.id.nil?
    - dropdown @account.name do
      - dropdown_item 'Settings', url_for(:controller => :home, :action => :edit_account)
      - dropdown_divider
      - dropdown_item 'Sign out', url_for(:controller => :home, :action => :logoff)
